#################################
# TeamOS picom (Openbox)
#################################

backend = "glx";
vsync = true;
use-damage = true;

shadow = true;
shadow-radius = 18;
shadow-opacity = 0.55;
shadow-offset-x = -14;
shadow-offset-y = -14;
shadow-exclude = [
  "name = 'Notification'",
  "class_g = 'Rofi'",
  "class_g ?= 'Dunst'",
  "window_type = 'dock'",
  "window_type = 'desktop'"
];

fading = true;
fade-in-step = 0.04;
fade-out-step = 0.04;
fade-delta = 4;

inactive-opacity = 0.93;
active-opacity = 1.0;
frame-opacity = 0.95;

corner-radius = 8;
round-borders = 1;
rounded-corners-exclude = [
  "window_type = 'dock'",
  "window_type = 'desktop'"
];

blur-method = "none";

wintypes:
{
  dock = { shadow = false; clip-shadow-above = true; };
  dnd = { shadow = false; };
  popup_menu = { opacity = 0.96; };
  dropdown_menu = { opacity = 0.96; };
};